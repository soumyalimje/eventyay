---
applyTo: 'app/**/*.toml'
---

# TOML configuration instructions

When adding or modifying tool configuration, check the version of the tool and do not suggest outdated configuration.
For example, we are using Pytest v9+, so do not suggest configuration for older versions of Pytest.
