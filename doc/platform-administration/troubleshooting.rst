Troubleshooting
===============

When diagnosing a production problem, first check application and worker
logs, database and Redis connectivity, reverse-proxy configuration, and the
status of migrations and static assets. The operational pages linked here
describe the supported maintenance commands and scaling considerations.

.. note::

   This page intentionally does not prescribe infrastructure-specific commands
   that are not present in the repository's deployment documentation.
